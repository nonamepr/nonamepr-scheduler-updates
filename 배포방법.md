# 휴대폰 웹앱(PWA) 배포 — GitHub Pages

앱스토어 등록 없이, 링크만 공유하면 직원들이 홈 화면에 추가해 앱처럼 씁니다.
**무료이고, 업데이트하면 즉시 반영**됩니다(직원이 아무것도 안 함).

---

## 1단계 — 저장소에 웹앱 올리기 (한 번만)

이미 만들어 둔 `nonamepr-scheduler-updates` 저장소를 그대로 씁니다.

1. https://github.com/nonamepr/nonamepr-scheduler-updates 접속
2. **Add file → Upload files** 클릭
3. `schedule-widget/web` 폴더 안의 **내용물 전체**를 끌어다 놓기
   - `index.html`, `app.js`, `styles.css`, `manifest.webmanifest`, `sw.js`
   - `icons` 폴더 (통째로)
4. 맨 아래 **Commit changes**

> ⚠️ `web` 폴더 자체가 아니라 **그 안의 파일들**을 올려야 합니다.
> (폴더째 올리면 주소에 `/web/` 이 붙습니다 — 그래도 동작은 합니다)

## 2단계 — GitHub Pages 켜기 (한 번만)

1. 저장소 → **Settings** → 왼쪽 **Pages**
2. **Source**: `Deploy from a branch`
3. **Branch**: `main` / 폴더 `/ (root)` → **Save**
4. 1~2분 뒤 페이지 상단에 주소가 나타납니다:

```
https://nonamepr.github.io/nonamepr-scheduler-updates/
```

---

## 3단계 — 직원 안내

아래 내용을 그대로 전달하시면 됩니다.

> **휴대폰에서 스케줄 확인·입력하기**
>
> 아래 주소를 휴대폰 브라우저로 여세요.
> https://nonamepr.github.io/nonamepr-scheduler-updates/
>
> **아이폰 (사파리)**
> 하단 공유 버튼 → **홈 화면에 추가**
>
> **안드로이드 (크롬)**
> 우측 상단 ⋮ → **홈 화면에 추가** (또는 "앱 설치")
>
> 홈 화면 아이콘으로 실행하면 앱처럼 열립니다.
> 회사 이메일과 비밀번호로 로그인하면 되고, **한 번 로그인하면 계속 유지**됩니다.

---

## 사용법

| 동작 | 방법 |
|---|---|
| 할 일 추가 | 하단 입력창에 쓰고 **＋** 또는 엔터 |
| 완료 체크 | 왼쪽 체크박스 |
| 상세 보기·메모 | 할 일 **글자 부분 터치** |
| 날짜 이동 | 상단 **‹ ›**, **오늘** 버튼 |
| 삭제 | 상세 화면 → 우측 상단 **삭제** |
| 로그아웃 | 우측 상단 **⋯** → 로그아웃 |

- 데스크탑 프로그램과 **같은 데이터**를 봅니다. 한쪽에서 입력하면 다른 쪽에도 나타납니다.
- 휴대폰에서는 **본인 할 일만** 보입니다 (관리자도 동일 — 확인·체크 용도이므로 단순하게 구성)
- 알림 기능은 없습니다.

---

## 수정할 때

`web` 폴더의 파일을 고쳐서 GitHub에 다시 올리기만 하면 됩니다.
직원들은 앱을 다시 열면 자동으로 최신 화면을 받습니다. (설치·업데이트 과정 없음)

> 화면이 옛날 것으로 보이면 브라우저를 완전히 껐다 켜면 됩니다.

---

## 문제 해결

| 증상 | 원인 / 해결 |
|---|---|
| 페이지가 안 열림 | GitHub Pages 활성화 후 1~2분 기다리기 |
| "이메일 또는 비밀번호가 올바르지 않습니다" | 데스크탑 앱과 동일한 회사 계정으로 로그인 |
| 할 일이 안 보임 | 상단 날짜가 오늘인지 확인 (**오늘** 버튼) |
| 홈 화면 추가가 안 보임 | 아이폰은 **사파리**에서만 가능 (크롬 불가) |
